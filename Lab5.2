SELECT 
    EMPLOYEE_ID,
    FIRST_NAME || ' ' || LAST_NAME AS EMPLOYEE_NAME,
    TO_CHAR(HIRE_DATE, 'DD Month YYYY') AS "START WORK",
    NVL(COMMISSION_PCT, 0) AS COMMISSION
FROM 
    EMPLOYEES
WHERE 
    SUBSTR(FIRST_NAME, 1, 1) BETWEEN 'C' AND 'H'
ORDER BY 
    FIRST_NAME;
